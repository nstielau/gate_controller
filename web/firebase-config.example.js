// Copy to firebase-config.js after `firebase init`. This browser config is
// public; MQTT credentials must only exist in Cloud Secret Manager.
export const firebaseConfig = {
  apiKey: "replace-me", // pragma: allowlist secret
  authDomain: "replace-me.firebaseapp.com",
  projectId: "replace-me",
  appId: "replace-me"
};
